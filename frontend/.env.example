# Copy to .env.production (or set in your host's build env) when the backend is
# deployed on a DIFFERENT origin than the frontend. No trailing slash.
# Leave unset for same-origin / reverse-proxy deploys.
VITE_BACKEND_URL=https://your-backend-host.com
